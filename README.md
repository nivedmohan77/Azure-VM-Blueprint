# Azure-VM-Blueprint
Terraform modules for rapid Azure VM deployment. Features include Ubuntu/Windows selection, non-overlapping VNet CIDRs, and Standard SKU Public IPs optimized for K8s lab environments.
